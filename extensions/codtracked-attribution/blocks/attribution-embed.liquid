{% comment %}
  CODTracked App Embed — capture UTM / click IDs into cart attributes.
  Merchant onboarding: Theme editor → App embeds → enable this embed → Save.
  Defaults to off after install (Shopify App Store theme extension requirement).
{% endcomment %}

{% schema %}
{
  "name": "t:name.attribution-embed.name",
  "target": "head",
  "javascript": "codtracked-attribution.js",
  "settings": []
}
{% endschema %}
