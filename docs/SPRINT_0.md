# Sprint 0 — Foundation & mock contracts

Completed scope (do not treat later roadmap items as done):

- Audited stack, legacy `(app)` routes, service-role usage, and ActionResult consistency.
- Centralized `ActionResult`, searchParams/pagination helpers, safer `toUserMessage`.
- Extended permissions for upcoming modules (integrations, operations, shipments, …).
- Added typed provider contracts, mock adapters, and server-only registry.
- Added `INTEGRATION_MODE` / `MOCK_INTEGRATIONS_ENABLED` (server-only).
- Extended RLS script with unauthorized write probes; added `npm run test:unit`.
- UI: `ErrorState` / `ForbiddenState`, `/forbidden`, `DemoModeBadge`.

**Not in Sprint 0:** full orders/logistics/reconciliation/analytics screens, live adapters, new operational tables, external API calls.
