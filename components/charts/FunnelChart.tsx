export interface FunnelStage { label: string; value: number; }
export function FunnelChart({ data }: { data: FunnelStage[] }) { const max = Math.max(...data.map((stage) => stage.value), 1); return <div className="space-y-3">{data.map((stage) => <div key={stage.label}><div className="mb-1 flex justify-between text-sm"><span className="text-text-secondary">{stage.label}</span><span className="font-medium">{stage.value.toLocaleString("es-ES")}</span></div><div className="h-7 rounded bg-muted"><div className="h-full rounded bg-brand-primary transition-all" style={{ width: `${(stage.value / max) * 100}%` }} /></div></div>)}</div>; }
